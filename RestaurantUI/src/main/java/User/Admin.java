package User;

public class Admin extends User{
    public String test = "ADmn";
    public Admin(String firstName, String lastName, String username, String password) {
        super(firstName, lastName, username, password);
    }
}
